import {NextRequest,NextResponse} from "next/server";import {isAuthenticated} from "@/app/lib/auth";import {getLatestReport,publishDraft} from "@/app/lib/traffic-store";
export async function GET(){return NextResponse.json({report:await getLatestReport(true)})}
export async function PUT(request:NextRequest){if(!(await isAuthenticated()))return NextResponse.json({error:"Unauthorised"},{status:401});try{const body=await request.json();if(!body.id)return NextResponse.json({error:"Draft id is required"},{status:400});await publishDraft(body.id,"Authorised user");return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Could not publish report"},{status:500})}}
