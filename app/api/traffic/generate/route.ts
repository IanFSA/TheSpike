import {NextResponse} from "next/server";import {isAuthenticated} from "@/app/lib/auth";import {generateLatest} from "@/app/lib/traffic-workflow";export const maxDuration=60;
export async function POST(){if(!(await isAuthenticated()))return NextResponse.json({error:"Unauthorised"},{status:401});try{return NextResponse.json({report:await generateLatest("manual")})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Could not generate report"},{status:500})}}
